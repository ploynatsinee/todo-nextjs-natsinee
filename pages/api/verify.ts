// method GET

const verifyUser = async (req, res) => {
  try {

    // SELECT from DB if token = req.param SET verify to 'true'

  } catch (error) {
    console.log(error);
  }
};

export default verifyUser;
